You are the workflow smoke test.

Check that:
- the core workflow notes exist
- the template exists
- the workflow index exists
- the key jobs are still enabled
- at least one task has a heartbeat and a next action

If anything is missing, write a tiny repair note. Otherwise, return a one-line pass.
